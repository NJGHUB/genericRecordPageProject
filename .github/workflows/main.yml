name: Salesforce Build Validation

on:
  push:
    branches:
      - main

jobs:
  validate-code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Salesforce CLI
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - name: Install Salesforce CLI
        run: |
          npm install -g sfdx-cli

      - name: Authenticate with Salesforce Org
        run: |
          sfdx force:auth:jwt:grant --clientid ${{'3MVG9wt4IL4O5wvKzBvv0aR2FivuC6iLpNxbIWzTaDt2jpFR3DyPniH5fXZcc_wPJaYIi4VWp2pE3xYhdXMAZ'}} --jwtkeyfile ./server.key --username ${{'wayto@perfection.com'}} --setdefaultdevhubusername -a HubOrg

      - name: Validate Metadata
        run: |
          sfdx force:source:validate --json --wait 10

      - name: Check Test Coverage
        run: |
          sfdx force:apex:test:run --codecoverage --resultformat json --testlevel RunLocalTests --json > test-result.json
        continue-on-error: true

      - name: Publish Test Results
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: 'Salesforce Code Tests'
          format: 'junit'
          path: 'test-result.json'
